import { styled } from '@mui/material'

export const Page = styled('div')({
  margin: '0 auto',
  width: '100%',
  maxWidth: 720,
  padding: '32px 0',
  display: 'grid',
  gridTemplateColumns: '1fr',
  gap: 32
})
